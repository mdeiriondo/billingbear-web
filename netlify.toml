[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Configuración para funciones serverless (SSR)
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/entry"
  status = 200
